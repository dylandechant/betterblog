<p id="notice"><%= notice %></p>

<h1>Users:</h1>

<ul>
    <%  @users.each do |user| %>
        <li><%= user.username %></li>
        <td><%= link_to 'Show Posts', user_posts_path(user) %></td>
        <td><%# link_to 'Destroy', user_path(user), method: :delete { confirm: 'Are you sure?' } %></td>
    <%  end  %>
</ul>
<%= link_to 'Back', :back %>
